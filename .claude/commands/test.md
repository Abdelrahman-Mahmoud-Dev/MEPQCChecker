Run all unit tests:

`dotnet test tests/MEPQCChecker.Core.Tests/MEPQCChecker.Core.Tests.csproj --verbosity normal`

If any test fails, read the failing test file and the class under test, then suggest a fix.
